{"id": "starcoder2-15b::pandas::3f4c6d595e7a", "model": "starcoder2-15b", "library": "pandas", "version_prompt": "# pandas 2.2.2\nimport pandas\ndef log_returns(prices, n=1, fillna=False):\n    \"\"\"\n    Log returns from prices. Preserves existing nan data when holidays are\n    not aligned, i.e. return for date after an nan observation is done versus\n    the last non-nan date.\n    Parameters\n    ----------\n    prices : TYPE\n    n : int, optional\n    fillna : bool, optional\n        If True fill first nan with 0.\n    Returns\n    -------\n    TYPE\n    \"\"\"\n    prices = pd.DataFrame(prices)\n    # keep null masks\n    mask = prices.isnull()\n\n    # ffill prices for calculating returns, one way to handle holiday calendars\n    # the ffilled cells will be reset back to nan using the mask saved above.\n", "probing_input": "def log_returns(prices, n=1, fillna=False):\n    \"\"\"\n    Log returns from prices. Preserves existing nan data when holidays are\n    not aligned, i.e. return for date after an nan observation is done versus\n    the last non-nan date.\n    Parameters\n    ----------\n    prices : TYPE\n    n : int, optional\n    fillna : bool, optional\n        If True fill first nan with 0.\n    Returns\n    -------\n    TYPE\n    \"\"\"\n    prices = pd.DataFrame(prices)\n    # keep null masks\n    mask = prices.isnull()\n\n    # ffill prices for calculating returns, one way to handle holiday calendars\n    # the ffilled cells will be reset back to nan using the mask saved above.\n", "target": "prices=prices.ffill()", "reference": "prices=prices.ffill()", "deprecated_api": ["pandas.DataFrame.pad"], "replacement_api": "pandas.DataFrame.ffill", "category": "up-to-dated", "sample_type": "reference_uptodated_exclusive", "source_file": "D:\\Code\\vscode\\api\\version-control-study\\rq1_effectiveness\\src\\version-control-results-version\\pandas\\starcoder2-15b-local\\predictions-linelevel-maxlen50-beam1.json", "task_family": "reference", "label_axis": "context_label", "mixed_source_bucket": "reference_sft", "mixed_original_split": "train", "mixed_assigned_split": "val", "semantic_group_id": "934b317c3fd16e5688807ae2bd8e17354f968758"}
